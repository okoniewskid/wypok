<%= stylesheet_link_tag "/assets/link.css" %>
<%= stylesheet_link_tag "/assets/avatar.css" %>

<%= link_to '', links_path, class: "btn btn-default fa fa-reply", title: "Powrót" %>

<%= form_tag(users_path, :method => "get") do %>
  <ul class="nav navbar-nav pull-left">
    <li>
      <div class="input-group">
        <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Szukaj" %>
        <span class="input-group-btn">
          <button type="submit" class="btn btn-default btn-outline"><span class="glyphicon glyphicon-search"></span></button>
        </span>
      </div>
    </li>
  </ul>
  </br></br>
  <% if @va %>
    <input name="view" type="checkbox" checked value="admin", onClick="this.form.submit();" />
  <% else %>
    <input name="view" type="checkbox" value="admin", onClick="this.form.submit();" />
  <% end %>
  Pokaż administratorów
  </br></br>
  <% if @sn %>
    <button type="submit" class="btn-link" name="sortName" value="asc">Sortuj wg nazwy (rosnąco)</button></br>
  <% else %>
    <button type="submit" class="btn-link" name="sortName" value="desc">Sortuj wg nazwy (malejąco)</button></br>    
  <% end %>
<% end %>
</br>
<%= will_paginate @users, :previous_label => 'Poprzedni', :next_label => 'Następny', renderer: BootstrapPagination::Rails %>
<%= render :partial => @users %>
<%= will_paginate @users, :previous_label => 'Poprzedni', :next_label => 'Następny', renderer: BootstrapPagination::Rails %>