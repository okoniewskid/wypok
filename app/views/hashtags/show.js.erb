$('#hashtagged').append('<div id="comments"><%= j render @hashtagged %></div>');
<% if @hashtagged.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate @hashtagged, renderer: BootstrapPagination::Rails %>');
<% else %>
    $(window).off('scroll');
    $('#infinite-scrolling').remove();
<% end %>